<List class="menu" aria-role="menu">  
  <Each data={@attrs.items}>
    <If data={@item.path} value={hierarchy.0.path}>
      <MenuItem class="is-active" />
    <Else />
      <MenuItem />
    </If>
  </Each>
</List>